# SP-RWCV?????????????

## ??
SCRF ????????????????????????? SCRF/RRF ???????? baseline ???? attention ??????????? homography warping??????????????

SP-RWCV ???????????????????????????????

## ????
??? stage?
1. reference feature ??? reference volume?????? 1?
2. ?? source feature ???? homography warping?
3. ???? warped volume ??? view reliability score?
4. ? score ???????
5. ??????????????
6. ??????????? cost volume?
7. ????? baseline ? 3D CNN regularization ? depth regression?

## ??
???? i?

`score_i = ScoreNet(F_ref, mean_D(F_i^w), |mean_D(F_i^w)-F_ref|, max_D corr, valid_mask)`

`w_i = 1 + gamma * tanh(score_i / tau)`

???????????

`w_i = 1 + gamma * conf_prev * tanh(score_i / tau)`

??????

`C = sum_j(w_j * F_j^2) / sum_j(w_j) - (sum_j(w_j * F_j) / sum_j(w_j))^2`

?? reference view ???? 1?

## ? SCRF ???
- SCRF/RRF?baseline cost volume + attentive cost volume + gate/residual fusion?
- SP-RWCV????? reliability-weighted variance cost volume?
- SCRF/RRF???????? warping?
- SP-RWCV??? source view ? warping ???
- SCRF/RRF????????
- SP-RWCV??????????????

## ????
- `models/modules/view_attention.py`
  - `SinglePassReliabilityWeightedViewAttention`
- `models/cas_mvsnet.py`
  - `DepthNet.forward()` ? `uses_single_pass_weighted_variance` ??
  - `CascadeMVSNet` ? `view_attention_mode='single_pass_reliability_weighted'`
- `train.py` / `test.py`
  - ?? `--view_attention_mode single_pass_reliability_weighted`

## ????
?????????

```bash
--use_view_attention --view_attention_mode single_pass_reliability_weighted
```

??? `--use_view_attention` ?????? baseline?
